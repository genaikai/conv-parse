"""배포마다 바뀌는 값을 한 곳에 모은다.

이 파일만 열어 보면 "다른 장비로 옮길 때 무엇을 고쳐야 하는가"가 끝난다.
전에는 서비스 오류 문구는 checks.py, 히스토리 턴 수는 conv.py, 조직 필드 후보는
org.py, 환경변수 이름은 backends.py 에 흩어져 있었다. 옮기는 사람이 그걸
전부 찾아다녀야 했다.

여기 없는 것도 바뀔 수 있다. 크기가 커서 원래 자리에 둔 것들이다:
    ragdiag/prompts.py    LLM 시스템 프롬프트 4종
    ragdiag/taxonomy.py   케이스 29개와 설명
    ragdiag/labels.py     query · emotion 라벨표 (실행 환경에는 그쪽 것이 있다)

**여기 값을 바꾸면 골든셋·회귀셋을 다시 돌릴 것.** 임계값 하나가 판정 전체를
움직인다. 특히 MATCH_THRESHOLD 를 낮추면 지어낸 인용이 통과하기 시작한다.
"""

from __future__ import annotations

# ---------------------------------------------------------------------------
# 서비스 자원 부족 응답  (case9)
#
# 모델 자원을 확보하지 못했을 때 서비스 계층이 답변 자리에 넣는 확정 문구.
# LLM 이 만든 답이 아니므로 코드로 대조한다. 배포마다 다르니 줄을 추가해 쓴다.
# 공백 · 줄바꿈을 지우고 부분 일치로 본다 - 앞뒤에 안내가 더 붙어도 잡힌다.
# 판정은 이 대조 하나다. 비슷한 문구는 잡지 않는다.
# ---------------------------------------------------------------------------

SERVICE_ERROR_TEMPLATES = (
    "서비스에 문제가 있거나, 사용자 분들이 많아서 서버에 부하가 걸리고 있어요",
)

# ---------------------------------------------------------------------------
# 입력 다듬기
# ---------------------------------------------------------------------------

# Step 1 에 넘길 이전 질문의 최대 개수.
# 대명사 해소에 필요한 것은 보통 직전 2~3턴이다. 그보다 오래된 질문은 노이즈에
# 가깝고 resolved_question 을 엉뚱한 주제로 끌고 갈 수 있다. 긴 대화에서는
# 입력 토큰도 턴 수에 비례해 늘어난다.
MAX_HISTORY_TURNS = 3

# 필터에서 "제한 없음"을 뜻하는 값들.
FILTER_ANY_VALUES = frozenset({"전체", "all", "ALL", "", "*"})

# ---------------------------------------------------------------------------
# 인용 검증  (사전지식 오염을 막는 핵심 임계값)
# ---------------------------------------------------------------------------

# 판정자가 근거로 제출한 인용의 최소 길이(공백 · 문장부호 · 한자 병기 제외). 짧은 인용은
# 아무 문서에나 우연히 들어맞아 검증을 무력화한다. 실제 검색 결과 모양(문서 10~15개 ×
# 약 500자)에서 다른 문서의 조각이 우연히 통과하는 비율은 8자 7.9% · 10자 4.4% · 12자 2.7% ·
# 16자 1.1% 였고, Haiku 가 실제로 낸 인용 314건 중 12자 미만은 없었다(최소 13자). 그래서 12.
EVIDENCE_MIN_QUOTE_CHARS = 12

# 완전 일치가 아니어도 통과시키는 최소 연속 일치 비율. 모델이 조사나 끝맺음을
# 살짝 다듬는 경우를 흡수하되, 문장을 새로 지어내면 통과하지 못하는 수준.
# 낮추면 지어낸 인용이 통과하기 시작한다. 비율과 별개로 조각이 원문과 한 글자 차이면
# 통과다 - 오탈자 1자는 비율로 못 살린다(0%). 규칙 전체는 verify.evidence_ratio 에.
MATCH_THRESHOLD = 0.9

# 판정자가 **발화**에서 따온 인용(불만 아님의 근거 · 요구 · 어긴 조건)의 통과 비율.
# 문서 인용(위)보다 낮다. 대조 대상이 짧은 발화라 오탈자 한 글자가 비율을 크게 깎고,
# 떨어졌을 때의 처리가 안전한 방향(case0 는 low 로, 요구와 ignored 는 철회)이라 문서
# 인용만큼 엄격할 이유가 없다. 골든셋 변형 실측: 0.85 에서 오탈자 1글자 98% 통과,
# 재서술 6% · 영어 혼용 0% — 0.8 부터 재서술이 15% 새기 시작한다.
QUOTE_MATCH_THRESHOLD = 0.85

# 발화 인용의 최소 길이. 2자 조각은 앞 질문들에 우연히 있을 확률이 14% 라(골든셋 실측)
# ignored 오탐이 그대로 살아남는다. 4자면 5% 아래. 요구 구절("표로")은 정당하게 짧아서
# 2자 - 오탐의 결과가 요구를 남기는 쪽이고, 그 구절이 질문에 우연히 있을 일은 드물다.
UTTERANCE_MIN_QUOTE_CHARS = 4
REQUEST_MIN_QUOTE_CHARS = 2

# 이 길이부터는 원문과 한 글자 차이(편집 거리 1)를 그대로 받는다. 연속 일치 비율은 짧은
# 인용에서 한 글자가 크게 깎이고, 특히 끝에서 두 번째 글자가 다르면 꼬리 한 글자가
# 일치로 안 세어져 8~10자 인용이 떨어진다 (골든셋 rq03 - 모델이 질문의 오탈자 "주새요" 를
# "주세요" 로 고쳐 인용). 6자인 이유: 편집 거리 1 을 허용한 우연 일치율이 4자 9.5% ·
# 6자 4.0% · 8자 0.8% 로, 6자가 4자 하한에서 받아들인 수준(4.5%)과 같다. 4자는 너무 새고,
# 8자는 "영어로 답해줘"(6자) 같은 요구 구절의 오탈자를 못 받는다.
QUOTE_EDIT_TOLERANT_MIN_CHARS = 6

# 답변이 인용부호로 제시한 문장을 원문과 대조할 때의 최소 길이 (case24).
# 위와 재는 대상이 다르다 - 이쪽은 답변이 주장한 인용, 위는 판정자가 낸 근거다.
ANSWER_QUOTE_MIN_CHARS = 10

# "짧게 답해줘" 의 기준값 (case11).
#

# ---------------------------------------------------------------------------
# 조직 분류
# ---------------------------------------------------------------------------

# 조직 분류 체계가 붙을 수 있는 로그 필드. 파일 이름으로 추측하지 않고 값을
# 대조해 고른다 - job_class 가 직무일 수도 직급일 수도 있고, 잘못 붙이면
# 에러 없이 전부 (미분류)가 되어 알아채기 어렵다.
ORG_CANDIDATE_FIELDS = ("db_dept_name", "db_job_name", "job_grade", "db_position_name")

# ---------------------------------------------------------------------------
# LLM 접속
#
# 앞에 있는 이름부터 찾는다. 실행 환경의 .bashrc 에 LLM_API_URL / LLM_API_KEY 가
# 있으면 인자를 하나도 주지 않아도 붙는다.
# ---------------------------------------------------------------------------

URL_VARS = ("LLM_API_URL", "API_URL", "RAGDIAG_BASE_URL",
            "OPENAI_BASE_URL", "OPENAI_API_BASE")
KEY_VARS = ("LLM_API_KEY", "API_KEY", "RAGDIAG_API_KEY", "OPENAI_API_KEY")
MODEL_VAR = "RAGDIAG_MODEL"

# ---------------------------------------------------------------------------
# 실행 기본값
# ---------------------------------------------------------------------------

CACHE_DIR = ".cache"
DEFAULT_WORKERS = 4
# ④′ 읽기 - 답변만 보고 "읽을 수 있는 글인가" 를 LLM 에 묻는다 (턴당 +1 호출). 코드 규칙이
# 못 잡는 붕괴(토큰 잡탕 · 중간부터 깨짐 · 프롬프트 유출)를 잡는다. 호출을 아끼려면 끈다.
LEGIBILITY = True
# 낙오자 기준. 한 단계에서 절반이 끝난 뒤, 중앙값의 이 배수(와 최소 초 중 큰 쪽)를 넘겨도
# 안 오는 턴은 에러로 닫고 다음 단계로 간다. 0 이면 끈다 (HTTP 타임아웃만 남는다).
STRAGGLER_FACTOR = 4.0
STRAGGLER_MIN_SEC = 60
DEFAULT_MAX_TOKENS = 16000
DEFAULT_TIMEOUT_SEC = 600
