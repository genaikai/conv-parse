"""기능 하나. 복사해서 쓰는 원본이다.

    cp -r src/<pkg>/features/template src/<pkg>/features/<기능>

그다음 `features/__init__.py` 의 `FEATURES` 에 한 줄 더하면 리포트에 나온다.
진입점·스키마·출력은 손대지 않는다 — 기능을 늘릴 때 만질 곳이 둘뿐인 것이 요점이다.

**이 폴더 자체는 지우지 마라.** 일곱 번째를 만드는 사람이 기존 기능 하나를 골라
베끼면 그 기능만의 사정까지 따라간다. 원본이 있어야 깨끗한 데서 시작한다.

지켜야 하는 것 둘:

- **`process_data(ctx) -> (metrics, notes)` 로 노출한다.** 등록부가 이 이름으로 부른다.
  낼 것이 없으면 빈 목록 둘을 돌려준다
- **지표 이름 앞에 `NAME` 을 붙인다.** 여럿의 결과가 한 리포트에 모이므로 접두어가
  없으면 겹친다. 이어 붙이는 줄(`("", ...)`)은 앞 지표에 딸린 것이라 예외다
"""

NAME = "template"  # 폴더 이름과 같게 둔다. 지표 접두어로 쓰인다


def process_data(ctx) -> tuple[list, list]:
    """이 기능의 판정 결과를 (지표, 노트) 로 돌려준다.

    ctx 는 `features/_context.py` 의 RunContext — 한 번의 판정이 남긴 것 전부다.
    실데이터의 개별 값·식별자는 넣지 않는다. 세는 것까지다.
    """
    return [], []
