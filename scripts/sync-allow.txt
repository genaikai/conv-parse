# C9 낱말 검사 예외. 고정 문구만, **여덟 글자 이상**.
#
# 길이 기준을 글자로 잡는 이유: 검사 쪽 awk 는 8 미만을 버리는데, 그 8 을 바이트로
# 세는 awk(macOS)와 글자로 세는 awk(UTF-8 로케일의 gawk)가 있다. 한글은 한 글자가
# 3바이트라 "외부 반출"(5글자·13바이트)은 개발 장비에서 살고 운영 장비에서 죽었다 —
# preflight 는 통과하는데 사본 점검만 실패하는, 가장 알아채기 어려운 어긋남이다.
# 여덟 **글자** 이상으로 적으면 두 해석에서 모두 산다.
#
# 이 프로그램이 판정하는 대상이 사내 보안정책 문답이라, 합성 데이터와 그 데이터를
# 쓰는 테스트에 "반출" 이 본문으로 들어간다. 워크플로를 드러내는 낱말이 아니라
# 판정 대상이므로 고칠 수 없다 — 고치면 합성 데이터가 실제 로그에서 멀어지고,
# 인용 검증 테스트가 이 문장을 그대로 쓰고 있다.
사내 자료의 외부 반출
외부 반출은 보안심의
외부 반출 심의 신청서
사내 자료를 외부로 반출
저장매체 반출 절차
모든 반출은 보안심의
모든 반출은 CISO

# 기술 용어. OpenAI 의 응답 형식 이름이다.
OpenAI 규격
